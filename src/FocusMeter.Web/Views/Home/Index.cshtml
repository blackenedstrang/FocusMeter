@{
	ViewBag.Title = "My FocusMeter Log";
}

<script type="text/javascript">
	 Ext.Loader.setConfig({ enabled: true });
	 Ext.application({
		 name: 'TP',
		 appFolder: 'viewscripts',
		 controllers: [
			 'UserController'
		 ],
		 launch: function () {
			 Ext.create('Ext.panel.Panel', {
				 renderTo: 'main',
				 layout: 'fit',
				 items: {
					 xtype: 'userlist'
				 }
			 });
		 }
	 });
 </script>
